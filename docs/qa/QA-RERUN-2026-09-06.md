# FieldQuo — QA rerun, night of 6 September 2026: readiness versus the morning

Production at the end of the night: commit `b9797cb`. Stripe still in TEST mode. Nothing deleted. All accounts except the owner's /platform login are test accounts, to be removed after the owner approves.

## Bottom line

**Nothing regressed.** Every morning finding was re-checked; the ones fixed during the day are confirmed fixed in production (the invoice family ledger, salaried pay, the daily-log 500, the services loading flash, the email-domain adoption refusal, the win-loss footer, the paid-date revenue). The rerun found **eleven new defects that were mine or blocked launch**, and every one of them was fixed, checked, built and deployed the same night. Four items need **you**, not code. Overall: **~90%**, up from ~87% this morning; the remaining gap is entirely owner actions and two product decisions.

### The rerun's own limitation, said plainly
The shared browser holds nine tabs and eighteen agents contended for them. About half the wave-one areas were verified from code and unauthenticated probes only. A second wave re-covered /platform, /app security, /sales security and the interrupted responsive and error sweeps **live on the final build**; the finisher's results are appended at the end of this file.

## What was fixed tonight (all deployed)

| Severity | Finding | Fix | Commit |
|---|---|---|---|
| CRITICAL | Quote creation and lead conversion 500'd for any company with a tiered quote: the shared allocator read `Q-2026-0014-G` as no sequence and re-issued `-0001` | One suffix-aware allocator; distinct tier suffixes; one transaction. Smoke: `Q-2026-0015` minted live after the tiered one | `5c9c560` |
| CRITICAL | Test company still pointed at `fieldquo.com`; the tenant's delete/repoint paths would have called Resend's delete on the platform domain | Both writes refuse a platform domain; row un-adopted | `114aad4` |
| HIGH money | "Choose plan" on a subscribed company opened a second subscription | In-place plan change (Stripe item swap, prorated, trial kept); page reloads on `changed` | `114aad4` |
| HIGH money | Request-payment email quoted $1,390.72 for a $1,190.72 balance on a pre-ledger amendment | Request-payment, lifecycle and PDF recompute the family first; backfill script (0 drifted in prod) | `dd1bc2b` |
| HIGH money | Two identical manual payments fired together both landed | Read-check-write in one transaction under an advisory lock; same-amount repeat within 15 s → 409. Smoke: 201 + 409 | `b9797cb` |
| HIGH | Cancel-consequences endpoint 500 on every call (Booking filter on a column it lacks) — cancel screen dropped "6 invoices, $5,530.73 unpaid" | Filter through event type. Smoke: 200 with counts | `114aad4` |
| HIGH | AI-credit bundle 502'd for every CAD company ("couldn't reach the payment provider" — false) | Refused before Stripe with the reason; Stripe's real message logged and forwarded; button off with the sentence. Smoke: 409 `currency_locked` | `114aad4` |
| HIGH | KPI dashboard said 3 won beside a win-loss page saying 5 (KPI route still pre-filtered by send date) | Same membership rule. Smoke: won 6, datedByDecision 2 | `114aad4` |
| HIGH | Rep calendar discarded every correction (lead snapshot won over typed values on every save) | Typed wins, snapshot fills, re-snapshot only when the lead changes; start past end refused; dates bounded to 2000–2100 | `114aad4`, `b9797cb` |
| MEDIUM | `/api/self-quote` bodyless 500 (the one intake the morning fix missed) | Guarded like its siblings. Smoke: 400 | `114aad4` |
| MEDIUM | `sales.fieldquo.com` claimable by a tenant | Reserved | `114aad4` |
| MEDIUM | Login, invite and platform-login had no rate limit | Ten per fifteen minutes per connection | `5c9c560` |
| MEDIUM | "Sent" status pill under 4.5:1 for seven brand colours | Measured wash pair | `5c9c560` |
| LOW | Voice page listed the AI wallet under a $0 voice balance; duplicate `US-TX`/`US-AZ` jurisdiction rows; floor sentence said "sales_voice"; two calendar buttons under 44 px; growth tables without a scroll wrapper | All fixed; `no-dupe-keys` is now a build error | `114aad4`, `5c9c560` |
| repo | `check:all` was red at HEAD from my own session (missing catalogue key ×9 languages, three stubs predating the ledger, a pin assuming it was last) | Green; three new checks added (`report-dates`, `growth-model`, `snapshot-cache`, `sales-calendar`, `quote-number`) | several |

Earlier in the day, also deployed: the reports' date fixes, the growth forecast, the one-number sales design, the DNCL flag, the bounded snapshot cache, and the 80-file lead bucket (see ROADMAP).

## Needs YOU (nothing here is code)

1. **Signup takes a card under a "First month · Free" headline** (HIGH, product). A company that closes the Stripe tab gets a fully working dashboard with no banner, then a 307 to `/signup` after 60 minutes. Decide: card-free trial, or say "card required" on the page and add the banner. I did not change it.
2. **Google Maps public key is not referrer-restricted** (HIGH, console). Anyone can run lookups on your bill. Set `GOOGLE_MAPS_SERVER_KEY` first (roof measurement falls back to the public key), then restrict the public key to `*.fieldquo.com`.
3. **Vercel env**: `BETTER_AUTH_URL` missing (warning on every auth request); `SALES_MAILING_ADDRESS` missing (signup-recovery cron does nothing); delete `TWILIO_PHONE_NUMBER`; set `FIELDQUO_SALES_TRANSFER_TO`.
4. **The r2.dev public URL** for the lead bucket — the only thing between the 1.32M rows and the pipeline.
5. **Price the AI-credit bundle in CAD** (or accept it is USD-only): a price decision.
6. **Sales payout batches can never be marked paid** — no control exists; "paid out to date" is a permanent $0. Tell me how you want to record a payout (manual button, or CSV import from your bank) and I build it.
7. Buy one 343 number with purpose **Sales**; Stripe live keys when you are ready; Meta App Review (prompt attached separately).

## Verdict by area — morning → tonight

| Area | Morning | Tonight | Basis |
|---|---|---|---|
| /app end-to-end loop | ship after X1 | **ship** — X1 confirmed on every surface; quote creation restored | live |
| /app quotes / invoices / payments | ship after X1 | **ship** — ledger reads recomputed; double-submit locked | live |
| /app jobs / scheduling / payroll | ship after H1 + H3 | **ship** — H3 confirmed live (salary only, $461.54) | live |
| /app settings | ship | **ship** — email-domain hazard removed | live |
| /app AI + billing | ship | **ship with two decisions** (card at signup; CAD bundle price) | live |
| /app analytics / reports | 75% | **~88%** — surfaces reconcile; spend screen still lifetime-only | live |
| /app clients / website / marketing | ship | **ship** — public surfaces live, /app surfaces code-verified | mixed |
| /app security / isolation | ship | **ship** — cross-tenant 404 from a real second tenant; reserved subdomains hold | live (wave 2) |
| /sales functional | ship | **ship** — calendar fixed; queue honest about zero prospects until the bucket URL arrives | live |
| /sales security | ship | **ship** — isolation, rate limit, calendar rules confirmed | live (wave 2) |
| /platform console + sales admin | ship | **ship** — growth page verified to the number; payouts still unpayable (yours) | live (wave 2) |
| Integrations / webhooks | ship after H4 | **ship** — Affirm settle executed; Maps key restriction is yours | mixed |
| Deployment / recovery / backup | — | **ship with known gaps** — schema matches prod exactly; rollback DB-free; Neon restore unproven from here | live |
| Performance / database / API | — | **ship** — 8-burst p50 0.23 s, no 5xx; unpaginated core lists noted | mixed |
| Responsive / a11y | mostly good | see finisher appendix | wave 2 |
| Usability / first run | — | **two decisions** (card; verification never surfaced) | live via curl |
| Browser matrix | — | only the embedded Chromium is testable here; a Safari/Firefox/Edge list is in `y-usability-first-run.md` | — |

## Still open, phase-able (none block launch)
Unpaginated core lists; seven multi-table routes without a transaction; `Company.timezone` unread by reports; spend screen has no date range; funnel and lead-import screens English-only; SVG logo upload answers 500 (Cloudinary rejects it, but the sentence is wrong); no Neon P1001 retry; `/api/auth/precheck` unthrottled; Twilio send failures not logged to /platform/errors; email verification never surfaced in /app; onboarding ticks "Set your pricing" for a trade priced by default; Stripe Checkout header shows the platform name (no `on_behalf_of`); quotes list prints pre-add-on totals (M13); accepted-add-on propagation. Full detail per report.

## Test data created tonight (nothing deleted)
Listed per report in each section 5. Notable: throwaway company `cmtpa5qpp000i0agmmon7tews` "QA Rerun Painting"; worker `cmtpa1nnw00030agmohy8iyz6` with an active $1,000/month salary (counted in overhead); several test-mode Checkout sessions; two emails to the permitted address; the growth assumptions saved on /platform (20/150/21, 15/5/40/5%, 0, 0).

## Appendix — wave-two finisher (live, production at b9797cb)

- **Error sweep:** 22 authenticated page loads across /app, /sales, /platform — zero 404/500, zero failed requests (one handled 400). `/api/self-quote` malformed body → 400 on all four public intakes now. One intermittent React hydration error (#418) seen once in 25 loads on `/app/quotes/new`, self-healing, page functional — LOW; needs a dev build to attribute.
- **Integration health:** ai-health healthy (`gpt-5-mini`); email-health healthy (`quotes@fieldquo.com`, 33 companies on the shared sender, none with a domain of their own); voice-health healthy, its one warning being wave one's own unsigned test call; billing-webhook errors: one pre-existing 26 August row needing manual reconciliation; no Affirm event has ever reached production. Meta page honest: "Not set up yet", no Connect button.
- **Responsive, rendered at 375 px:** `/app/marketing` still overflows (658 px wide) and the job header's action group still runs to 439 px — both morning findings, both still open, both cosmetic. `/login` is label- and keyboard-clean.
- **Focus ring (MEDIUM, a11y):** measured 2.24:1 dark / 2.89:1 light at 50% alpha; full-strength ring measures 4.99 / 12.14. **Fixed tonight** (`app/globals.css`, ring at full strength).
- **New env finding (MEDIUM):** `SALES_MAILING_ADDRESS` unset in production — signup-recovery emails are withheld for CASL; one Vercel variable.
- LOW: two `<h1>`s on settings pages; `/api/ui-state` fetched four times per load; a 400 from minimum-price logged on every new-quote load when capacity isn't set; a repo path ("docs/META-ADS-BUILD.md") shown on a contractor-facing page; Invoices page "Outstanding $28,143.22" versus consequences' "$5,529.73 / 6 unpaid" — different filters, worth one look.
