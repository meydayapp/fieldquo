// app/platform/audit-log/page.js — placeholder, see app/components/platform/NotBuilt.js
import NotBuilt from "@/app/components/platform/NotBuilt";

export default function PlatformAuditLogPage() {
  return (
    <NotBuilt
      title="Audit log"
      description="A record of platform-admin actions, including impersonation. Rows are already being written to PlatformAuditLog — there's just no screen to read them."
    />
  );
}
